# WGS — Women's Growth Society Content Studio

AI-assisted Instagram carousel/post generator for a single creator brand (WGS). Full spec lives in:

- `docs/blueprint.md` — product design, taxonomy, generation pipeline, design system
- `docs/implementation-guide.md` — folder structure, Pydantic contracts, phase-by-phase build plan

**Always read both docs in full before starting or resuming work on any phase.**

---

## Current status

- ✅ Phase 1 (Design foundation) — complete, verified PASS
- ✅ Phase 2 (Data spine) — complete, verified PASS
- ✅ Phase 3 (Generation) — complete, verified PASS
- ✅ Phase 4 (Surfaces) — complete, verified PASS
- ✅ Phase 5 (Mobile flow) — complete, verified PASS
- ✅ Phase 6 (Deployment) — complete, verified PASS. Live at https://wgs-studio.vercel.app (backend: https://wgs-backend-production.up.railway.app).

All six phases are done. Since Phase 6, on top of the original scope: a Supabase-Auth login screen gating the whole app + RLS on `brand_kit`/`memory`/`image_cache`; `memory`/`brand_kit` migrated off local disk to real Supabase reads/writes (survives a Railway restart, verified); a citation/grounding bug fix so `requires_citation: true` taxonomy topics stay grounded in the accepted angle instead of drifting to unrelated content (logbook #14/#15) — deployed and verified live in production; both Vercel and Railway now auto-deploy on push, scoped correctly (`watchPatterns`) and health-check-gated, after a real outage (logbook #20) got root-caused and fixed rather than just contained (logbook #23); `brand_kit`'s RLS was found wide open (`authenticated_full_access` — any authenticated session, full read/write) and tightened to service-role-only, with a database-level `audit_log` added for both `brand_kit` and `memory` (logbook #34); a real export-confirmation event built for the first time — `memory` gained `caption`/`slides`/`exported_at`/`voice_trained_at`, the voice-compounding mechanism (blueprint Section 4) actually fires now via an explicit opt-in toggle, and the masthead counter (stuck at `NO. 01` since Phase 6, since nothing had ever flipped a record to `exported`) is fixed as a direct consequence (logbook #35); `refine_post` compounding a `critique_post` false-positive into unwanted slide-count drift on fixed-slide-count formats, root-caused and fixed (logbook #29/#37); a content-drift root cause fixed — `voice_samples.direct` was uniformly workplace-themed and pulled unrelated topics toward invented office scenes, revised to be domain-diverse instead, a deliberate deviation from a blueprint-locked value, now confirmed live in production (logbook #30); the masthead simplified to `masthead_short` only, dropping the rule and `{category} NO. {n}`, a deliberate deviation from blueprint.md Section 12, now confirmed live in production (logbook #32); and one closed non-issue — the hero-image cache's low hit rate (logbook #13) was investigated and found no longer to be a real gap, closed rather than carried forward as open work (logbook #36). One known, deliberately deferred gap: `PicksStore` is still unconditionally file-backed (unlike `MemoryStore`/`brand_kit`/`image_cache`/`audit_log`), fully investigated and classified as low-stakes/suggestion-tier state rather than fixed — small, known migration scope if picked up later (logbook #38). A carousel-only "v1" content-voice change is live as of logbook #39 — a connected micro-essay arc (one historical/cultural/literary anchor, held throughout, with a caption that mirrors the whole arc) replacing the generic per-slide specificity/actionability/saveability checklist, plus a restricted 2-approach (`story`/`question_reflection`) sampling pool for carousel — gated entirely on `format == carousel`, with `single_image` completely untouched. Five rounds of real-output review have now run (each against real `/generate`-pipeline calls, real Anthropic API): round 1 found critique-truncation, CTA-flagging waste, and kicker/anchor-word drift; round 2 solved the CTA issue but only partially fixed truncation and the closing-declarative rule; round 3 diagnosed the closing-question override (traced to an "an open question is a valid ending" line in the shared system prompt — left unfixed by choice, reported rather than guessed at) and got truncation and closing-declarative both holding 5/5; round 4 added a direct `refine_post`-side backstop for the closing-declarative rule (matching the #29/#37 precedent); round 5, found by *reading the actual generated prose* rather than any structural test, caught a second reader-address leak — refine dropping an unhedged question into the anchor-introduction body slide itself, same underlying ambiguity as the closing-question bug, fixed with the same three-part pattern (arc instruction + critique checklist + refine backstop) applied to that location; round 6, found through real organic use of the live app rather than any session-driven test, fixed an anchor-swap bug and added a hedge floor, with verification deliberately deferred to continued live use from that point on; round 7 made the first **structural** (not prompt-only) change in this line of work — a real `carousel_conversation` slide (the CTA/question slide matching the locked hand-written v1 reference: a fixed `- Conversation for today` label, one model-written open question tied to the post's anchor, a fixed `I'd love to hear it.` invite) now exists after the closing slide for every carousel post, bumping carousel's slide count for the first time (4–5 slides, not 3–4) — implemented and locally verified (real Satori render, full backend suite). The label originally led with a 🌿 emoji; real-render testing found it (and em dash/middot/hedera/star alternatives) all render as tofu in this project's locked font set — fixed by switching to a plain ASCII hyphen, verified rendering cleanly the same way. Round 8 made five more confirmed, decided changes: body slides raised 1–2 → 3 (fixed regardless of approach, 6 slides total now — cover, 3 body, closing, conversation), the hardcoded "with you," dropped from the closing slide's display (backend value unchanged, #32's display-only pattern), the real `brand_kit`-driven "Follow us..."/`@handle` footnote relocated from the closing slide to the conversation slide (the true last slide as of round 7 — it had been a leftover from before that slide existed), anti-padding/split guidance added to keep the extra body-slide room from being padded (adapted from an audited pattern in another project, not invented fresh), and a 10% word-budget tolerance added to the system prompt and critique (deliberately not `validator.py`, which stays a hard check). Still not yet run through a real `/generate` call. This is still an **active, unverified experiment** on the actual content-quality question, not a finished feature — the deferred quality read still needs the same close-the-loop treatment as #30/#32. Full detail, plus every other post-deploy fix, is in `docs/logbook.md` — that's the source of truth for "what's actually happened," not this section.

Separately, alongside the production carousel prompt above, an isolated **direct-write POC** now exists at `/poc` (`POST /poc/generate`, `backend/app/poc/`) — a topic name goes straight to one model call using a standalone system prompt (`backend/app/poc/prompt.py`, the **active** prompt, iterated on repeatedly since it was first written — see below) built from v2's principles (v1/v2 = the hand-written reference pieces this all traces back to — never the production carousel prompt, which is never called v1 or v2), with no taxonomy/`seed_angle`/`approach`/`entry_point`/mood sampling and no critique/refine loop. Built because real testing found this direct-write approach outperformed eight rounds of patching the production carousel prompt. **The default model provider is now `gpt-5.5` (OpenAI)**, changed from the original Claude/Sonnet path on real A/B evidence — on identical topics, `gpt-5.5` avoided two unhedged/questionable-anchor failures Claude produced (a medieval-guild wage claim, a disputed "Persian flaw" rug legend) and showed tighter beat-count discipline (6 slides in 3 of 5 trials vs. Claude's near-uniform 7). The Anthropic path remains fully functional via `provider="anthropic"`, unchanged, for whenever comparison testing continues. A second prompt implementing an externally-proposed "GPT editorial-architecture" (explicit multi-axis anchor scoring + journey staging + a light self-check, `backend/app/poc/prompt_gpt_variant.py`) was built and A/B tested against the active prompt — **evaluated and rejected**: it produced measurably worse anchor repetition (3/3 outputs converged on the same anchor vs. 1/3 for the active prompt, including near-verbatim repeated openings) and fixed none of the issues found in live UI testing. Kept in the codebase for reference only, not in active use (same practice as other paused/rejected approaches — see logbook #26). The prompt was then tightened for voice (rules 1/4/11: an earlier reader-signal, role-only citations instead of naming real researchers, and a ban on both demographic generalization — "many of us" — and hedgy reader-address — "if you looked closely, you might notice") after real-output review kept finding an essay-like, audience-addressing register; two of the four locked reference examples (Shimenawa, Shmita) needed light edits too, since they still modeled the exact patterns the new rules forbid. Separately, captions were consistently reading better than slides across nearly every testing round — traced to slides being drafted first (no hindsight on the whole arc) and carrying more independent structural constraints — fixed by reordering the output so the caption is written first as the real piece, with slides derived from it second. The first fix shipped (commit `81c632d`) made slides a byte-for-byte mechanical split of the caption's own sentences ("the slides are the caption, split") — this closed the quality gap but, on direct pushback the same session ("why is caption and slide content exactly same? caption needs to be a different perspective right?"), was found to have deleted the caption's actual reason to exist, since both are visible in the same Instagram viewing session. Revised so slides retell the caption's same beats/anchor/order in fresh wording instead of copying it — verified across three real trials with no verbatim or near-verbatim sentence reuse — while keeping the caption-first draft order that fixed the underlying quality gap. Two more rules (12, 13) were then added for reading experience: punctuation/pacing guidance (periods for real stops, commas only for a light breath, em dashes for turns) and an optional single line break per piece for a short phrase that's earned its own pause — rule 13 needed one round of hardening after a soft "sparingly" version fired in 3-5 of a piece's slides instead of the intended rare use, tightened to a hard one-per-piece cap with an explicit count-and-check step. `PocParagraphSlide.tsx` (the live `/poc` preview, plain React/DOM, not Satori) gained `whiteSpace: "pre-line"` so an embedded line break actually renders. Both changes shipped as commit `ae529a0`, confirmed live via a real production call (reworded slides, clean punctuation, zero line breaks fired — the expected compliant default). The POC's first real UI test surfaced a genuine layout bug (slides rendering side-by-side instead of one-at-a-time with swipe/dot navigation), now fixed. Anchor-repetition is a known, deliberately deferred gap — the POC is stateless by design, and the `gpt-5.5` A/B test found a real cross-model anchor convergence (the same "first/second sleep" historical fact independently chosen by both models on different topics), suggesting the gap isn't purely per-model. Full reasoning, the complete terminology, the full current POC prompt, and testing history across every round: `docs/direct-write-poc.md`.

The POC's provider evidence has since been carried into the **production pipeline itself**, not just `/poc`: `app/providers/llm.py`'s `LLMProvider` now defaults every caller (angle engine, daily-pick pitches, single_image draft/critique/refine, paste-link summarization) to `gpt-5.6-luna`/`gpt-5.5`, with the Claude path kept fully functional behind an explicit `provider="anthropic"` opt-in — see the Locked Decisions entry above and logbook for the full migration, including a real, previously-latent bug this work surfaced (OpenAI's reasoning-token consumption silently returning empty output at this codebase's real production token budgets) and its fix.

**OPEN, EXPERIMENTAL — the POC's approach has now been ported into the production carousel writer itself** (`engine/generator.py::draft_carousel_direct()`, logbook #43/#44), replacing `draft_post → critique_post → refine_post` for carousel only — `single_image` is completely untouched. One strong-tier call, free anchor pick guided by `angle_engine.assemble_carousel_context()` (category + all of `topic.seed_angles` + knowledge_hints + a real, memory-backed anchor avoid-list — no more `sample_cell`/`generate_angle`, no `approach`/`entry_point` on this path at all), caption written first, with cover/closing fields written as explicit, deliberate JSON fields the same way `conversation_question` already was. No critique/refine call — reasoning and an explicit small-sample-size caveat are in the code and the logbook entry, not assumed away. **Body slides route to `carousel_body_teaching`, not `carousel_body`** (logbook #44) — confirmed via a direct investigation that the template was already real, live-reachable code (the still-live old carousel path hits it whenever `STORY` is sampled), not dead code, so the port moved onto it for the extra room (35-50 words vs. 10-20): three of the caption's real beats are now retold fresh (same pattern the POC itself already validated for reworded slides), each with a short, deliberately-written heading, rather than compressed into `carousel_body`'s short-statement shape as #43 first shipped. This also surfaced and fixed a real cross-file dependency: `validator.py`'s independent `slide_roles_for(brief)` call needed the caller's hardcoded `approach` switched from `QUESTION_REFLECTION` to `STORY` (the sole approach shared by `CAROUSEL_V1_APPROACHES` and `TEACHING_BODY_APPROACHES`) to stay in agreement with what the writer now actually produces. Verified with real API calls and real Satori renders across both rounds: a fresh collision-prone topic set (Burnout, not Boundaries) confirmed domain-differentiated content even where anchors converged — **cross-topic anchor convergence itself is a known, still-open, still-unfixed gap** (`career-burnout`/`wellness-burnout` both independently chose "canary in a coal mine"; tracked in `docs/logbook.md`'s deviations table, row 43-46, and `backend/app/poc/FINDINGS.md` #1, whose own "hasn't touched the real pipeline" framing is now stale for this specific finding since this port wires the same approach into production); a real before/after pair confirmed the anchor avoid-list actually changes the model's pick; knowledge_hints grounding held clean on 3 more citation topics; no overflow at the new wider range across 4 more fresh topics, though 2 of 4 tripped the new word floor (a real mild-undershoot tendency, confirmed via render to be a genuine visual difference, not a false positive — **fixed in logbook #52** by adding an explicit count-and-check self-check step, the same pattern already hardened for the POC's own rule 13; re-verified with 5 fresh real trials afterward, all 15 body slides landing inside range, zero validation errors); real cover/body/closing renders read cleanly with on-brand headings and zero closing-question drift. A direct follow-up check (5 more fresh topics) confirmed the body-slide retelling holds up narratively rather than reproducing the isolated POC's first failed "reshape" attempt (verbatim copying in 1 of 3 early trials) — zero full-sentence verbatim reuse across 15 slides checked, consistent 3-act compression shape trial to trial, one minor 3-word near-verbatim phrase flagged, not glossed over. **The writer now also produces a real hero image** (logbook #45) — a model-written `visual_subject` field (same call, same pattern as mood/cover fields/`conversation_question`), wrapped through the existing, reused (not rebuilt) `brief_builder._hero_image_prompt()` — the same function `generate_angle()`'s own visual_subject already goes through. Verified with 4 real images from the real GPT Image 2 + duotone pipeline, not just a prompt string: all read as genuinely on-brand (abstract, editorial, textural, zero faces or people, no uncanny-face risk), each specifically tied to its own anchor, including a tasteful result on a citation-required health topic. Full backend suite 132/132 throughout, `validator.py` confirmed unchanged. **Now wired into `routes/generate.py` as the real path** (logbook #46) — `CAROUSEL_WRITER=direct_write` is the new default for every fresh (non-preselected) carousel `/generate` call, replacing the legacy chain for that call; `CAROUSEL_WRITER=legacy` is the opt-in fallback, same reversible pattern as `LLM_PROVIDER`, not a hard cutover (renamed from an original `v1` value in logbook #47 — `docs/direct-write-poc.md` locks "v1"/"v2" to mean only the hand-written reference pieces, e.g. the shimenawa/Shmita examples referenced elsewhere in this section, never a pipeline or code path; this flag's old chain is "legacy," not "v1"). A preselected angle forces legacy. Until logbook #50, that covered every carousel generation the frontend could trigger — `goToGenerate(topic_id)` (used identically by a daily-pick tap and a plain category-browse tap) discarded the daily pick's own precomputed hook/angle and routed to `GenerateScreen`, which always called `/generate/propose` first and always submitted that fresh proposal as preselected (logbook #49's finding, from directly tracing the tap-to-generate flow, not assumed) — meaning direct-write was wired in but genuinely unreachable from any live UI tap. **Fixed in logbook #50**: `GenerateScreen` now skips the propose/preview step entirely when `format === "carousel"` and calls `POST /generate` with no preselected fields at all, so `preselected` really is `None` and `run_generate` reaches `_generate_carousel_direct` — confirmed by an actual browser tap-through, not code review (real local frontend + backend, a temporary diagnostic log line, then reverted with zero residual diff). `single_image`'s propose-then-accept flow is completely untouched and was separately re-confirmed live in the same session (real `/generate/propose` call, then `_generate_for_brief`/legacy `generate_post` on Generate). Carousel's loading state now shows staged reassurance text as elapsed time passes, since direct-write's sequential (not parallel) hero-image generation makes it slower end-to-end than legacy — see the Section 15 deviation below. Explicitly checked, not assumed: `engine/selector.py`'s daily-picks coverage/diversity weighting never reads `approach` from memory at all (only `topic_id` and `category`), so hardcoding `approach=STORY` as plumbing can't break it — though direct-write's memory records do carry a differently-shaped fingerprint (`topic_id:anchor` vs. the old `topic_id:sub_concept:approach`), so its history doesn't feed back into `single_image`'s/legacy's own non-repetition check, a real separate finding, flagged not fixed. **A real, named deviation from blueprint Section 15's "text and image generation run as independent parallel lanes" design, tracked in the deviations table (row 43-46), not just described generically:** direct-write's hero image can't generate in parallel with the text the way legacy's can (mood/visual_subject aren't known until the single writer call returns), so it's slower end-to-end, not faster — legacy and `single_image` are both unaffected and still run their lanes in parallel exactly as specced. Verified with real end-to-end calls against real Supabase (not each piece in isolation) on one non-citation and one citation-required topic — both produced a complete, validated post, a real hero image, and a `MemoryRecord` whose `anchor` was read back correctly from a fresh query. Daily-picks/selector confirmed to behave sanely against real current memory afterward. Full backend suite 135/135. **Ported to paste-link's own entry point too** (logbook #51) — `/generate/from-brief` (paste-a-link's route, not `/generate`) now also respects `CAROUSEL_WRITER` via a new `run_generate_from_brief()` branch point, deliberately kept separate from the shared `_generate_for_brief` (which `run_generate`'s own legacy/preselected fallback still calls unchanged, preserving logbook #46's "preselected always uses legacy" boundary). Not a naive reuse of the taxonomy prompt: paste-link has a real pinned article (`brief.sources`), not a topic to freely recall an anchor from, so a dedicated `_carousel_direct_paste_link_system_prompt()`/`draft_carousel_direct_from_source()` swaps the taxonomy prompt's free-recall anchor rule for one requiring the anchor to be a real detail already stated in the pinned excerpt — reusing the taxonomy prompt's anchor rule as-is here would have shipped a real grounding regression. Citation grounding itself is not rebuilt (`_citation_instruction_block`'s already-correct `"sources"` branch, logbook #12, is reused verbatim). The anchor avoid-list is deliberately not ported — investigated, not assumed: it's keyed by a recurring `Topic.id`, and paste-link's `topic_id` is a one-off per-article hash, so there's no `Topic` to look one up from at all. A real, non-obvious bug was caught by tracing `validator.py`, not assumed away: paste-link's default `brief.approach` (`STAT_RESEARCH`) isn't in `TEACHING_BODY_APPROACHES`, so `slide_roles_for(brief)` would compute the wrong (narrower) word-range check against the actual `carousel_body_teaching`-shaped slides — reproduced live (removed the fix, got the exact predicted validation failure, restored it) and fixed the same way logbook #44 fixed the same class of bug: force `approach=STORY` after the writer call returns. Verified with a real trial, not code review alone — a real Wikipedia article on impostor syndrome, pasted and generated through the real local backend: every specific claim in the output was fact-checked line-by-line against the actual fetched excerpt with zero fabrication found, the citation check passed cleanly, and the hero image rendered as a genuinely on-brand, anchor-specific image. Both the sequential-image tradeoff and the cross-topic anchor-convergence gap (deviations table, row 43-46) now apply to this entry point too — not a new deviation, the same one confirmed at a second entry point. Full backend suite 137/137. **`draft_carousel_direct()`'s cover/body/closing prompt and schema were then rewritten outright (logbook #53)** — replaced, not layered on top of the old instructions, since layering was exactly the checklist-accumulation failure mode this whole direct-write line of work exists to escape. Cover is now exactly `headline` (a short phrase, still the same 96px slot) + `cover_body` (1-2 real sentences that spell out an unfamiliar named thing's category, build curiosity by withholding meaning not facts, and add an explicit bridge to the reader's life only when the anchor illustrates a separate theme rather than being the subject itself). Body slides drop `heading` entirely in favor of `accent_phrase` (an exact substring of that slide's own `body`, rendered emphasized in-line). `closing_takeaway` is now a real 2-4 sentence build meant to leave a new feeling, not legacy's one-line echo of the caption's opening. `CoverSlide`/`BodyTeachingSlide` (shared render models with legacy) gained these as new optional/defaulted fields rather than anything being renamed out from under legacy, which is untouched and still uses the old shapes; `validator.py` gained a `carousel_writer`-gated word-range override so legacy's own shorter cover/closing ranges are undisturbed. Real render verification (not just word counts) found and fixed a genuine Satori bug along the way: `accent_phrase`'s word-flex highlight technique left a stray gap before punctuation glued directly onto it with no source space (e.g. "convenient .") — fixed by folding that punctuation into the accent span itself. Verified with 5 real, unmocked trials across 5 topics (one deliberately an anchor-IS-the-subject case) plus real Satori renders of every cover/closing/body slide: zero validation errors, genuine multi-word headlines with no overflow, 2-4 sentence closings fitting cleanly, and the bridge-line instruction generated for the first time — working directionally (fired correctly, and correctly stayed silent on the anchor-is-subject case) but not yet reliably 5-for-5. Cross-topic anchor convergence recurred again in this same trial batch, unprompted — still open, not fixed. Full backend suite 143/143. **The bridge-line instruction was then hardened (logbook #54)** — its soft "add one explicit sentence" ask (2/4 hit rate in #53's testing) rewritten with the same requirement-plus-checkable-self-check pattern already proven for rule 1 and the body-distillation word floor (#52): "This is a requirement, not an optional nicety... before finalizing cover_body, check: ... If it does not, add one now." Backend suite still 143/143 (prompt-text-only). Real-trial re-verification was blocked at first — both OpenAI (`insufficient_quota`) and Anthropic (`credit balance too low`) were out of credits, confirmed by direct live calls, not assumed. **Closed out once credits returned**, on direct instruction to trim the trial script down to 2 topics going forward (a new standing rule for this script, not the larger batches used in #43-53) and report the real number honestly: 2 fresh topics (`mindset-perfectionism`, `career-pay-scale`, neither used in #53's trial) — bridge sentence fired in both (2/2), up from 2/4 pre-fix. Small sample, reported as such, not rounded up into a reliability claim the sample doesn't support. Full backend suite 143/143 throughout (no app code changed between the blocked attempt and the closed-out run). **The closing slide (S5) was then changed to derive from the caption's actual ending instead of being written as a separate from-scratch field (logbook #55)** — the same derive-not-invent pattern the body slides already used, since real testing kept showing the caption's own ending read stronger than what a separately-instructed `closing_takeaway` produced; the old "new feeling... hasn't already stated outright" framing was removed since it directly contradicted deriving from what the caption already says. The swipe-editor's closing-slide edit form was also fixed from a single-line `<input>` (a leftover from before #53 made `takeaway` a real 2-4 sentence value) to the same `multiline` textarea `Question`/`Quote` already use. Verified on 2 fresh trials (`wellness-sleep`, `mindset-rest`): both closings clearly derived from the caption's real ending, genuinely reworded — though `mindset-rest` showed a real, honestly-flagged near-verbatim 4-word overlap ("It struck the hour"), not a full-sentence copy but worth watching. UI verified via a real headless-Chromium screenshot of a faithful static reproduction (`AuthGate` blocks driving the real page in this session, same constraint logbook #13 already hit) — confirmed the full value is present and unmodified in both the old input and new textarea (nothing was ever truncated at the data level), and the textarea genuinely wraps and is scrollable/resizable where the input just clipped mid-word. Full backend suite 143/143, clean frontend typecheck. **A full aesthetic pass then followed (logbook #56)** across every template the direct-write port actually uses (cover, body-teaching, closing, conversation — not `CarouselBody`/`single_image`, consistent with this whole arc's scoping): airier line-heights throughout; `CarouselBodyTeaching`/`CarouselClosing`/`ConversationSlide` switched from vertically-centered content to top-anchored (matching `CarouselCover`'s own convention) — **a real, named deviation from blueprint.md Section 12's "center within that wrapper" convention**, logged not silently reverted; body-teaching's font size also bumped 36px→44px (static, not responsive) since even the 55-word ceiling still left roughly half the frame empty at 36px; a cover swipe marker added ("Swipe >", plain ASCII — a first CSS border-triangle attempt rendered as a filled square instead of a triangle on a real Satori render, Satori doesn't replicate a browser's border-miter clipping, fixed by dropping the shape and using text only, same fallback-to-ASCII pattern as logbook #39's emoji-glyph fix); closing's weight dropped 700→600 and its font size is now responsive to word count (48px→36px linearly across the real word range) rather than fixed at one size. Since all four templates are shared with the legacy chain, these render changes apply to legacy-produced carousels too, by construction. Verified with real render checks at both the floor and ceiling of every template's actual word range (exact, programmatically-verified word counts, not hand-counted) plus 2 fresh real trials (`career-imposter-syndrome`, `wellness-burnout` — the latter repeating the already-tracked `canary in a coal mine` anchor-convergence gap, unprompted). One real overflow found and characterized: the cover's hero image clips at an unrealistically long (4-line) headline combined with a maxed cover_body — confirmed clean with a headline length matching what real trials actually produce, so flagged as a narrow edge case, not fixed. Honestly noted, not fixed: body-teaching at its true word-count floor still leaves visible trailing space below the text, an inherent tradeoff of a fixed canvas with a real content-length floor, not eliminable by a static font/leading change alone. Full backend suite 143/143 (no backend code changed), clean frontend typecheck throughout including after the swipe-marker fix. **The swipe-editor then gained a real edit form for `carousel_body_teaching` slides (logbook #57)** — a pre-existing gap flagged twice before (logbook #19, #53) and never fixed until now: `SlideEditForm`'s `switch` had no case for this template at all, so the 3 body slides of every carousel (legacy- or direct-write-produced) had no edit form. Fixed with the same `Field`/`cardStyle` pattern every other case uses — Heading (legacy-only), Body (multiline), Accent phrase. Verified with a real *functional* check, not a render pass: since `AuthGate` blocks driving the real authenticated `/editor` page in this session, used a structurally-valid but cryptographically-unverified fake Supabase session injected into `localStorage` (accepted because `AuthGate` only calls `getSession()`, which resolves from local storage with no network round-trip when unexpired — no real credentials touched, and nothing exercised here makes an authenticated Supabase call) plus a real `GenerateResponse` seeded into `sessionStorage` the same way `session-store.ts` writes it. A real headless Chromium then loaded the real page past `AuthGate`, confirmed the new form fields appear, edited `body` and confirmed the change reached real React state and the live `SlideRenderer` preview, and edited `accent_phrase` to a different real substring and confirmed — via actual computed CSS color on the real rendered DOM — that the highlight genuinely moved from the old phrase to the new one. Caught and fixed a real bug in the test itself along the way (a position-based locator silently grabbed the wrong input), and correctly did NOT treat an accent_phrase-not-found-in-body case as a bug, since that's `CarouselBodyTeaching.tsx`'s own documented task-"#19" fallback (plain, unhighlighted render). Full backend suite unaffected (143/143), clean frontend typecheck.

*(Update this section whenever status materially changes — a phase completes, or a significant post-deploy fix lands. Keep it a short orientation summary; put the real detail in the logbook.)*

---

## Phase-gating rule — non-negotiable

After finishing any phase: invoke an independent check against that phase's "Done when" criteria (see note below on `phase-verifier`), report pass/fail per criterion with concrete evidence, then **stop**. Do not begin the next phase without explicit confirmation, even if verification passes.

**Known environment note:** the custom `phase-verifier` subagent (`.claude/agents/phase-verifier.md`) is not recognized by this session's Agent tool — only built-in agent types are available. Use `general-purpose` instead, with the exact phase-verifier persona and instructions pasted in. Same independent, skeptical, read-only check — just a different dispatch path. Don't keep re-attempting the custom subagent; this is a confirmed limitation, not a fixable bug.

---

## Logbook discipline — non-negotiable

Every time a change is made to fix a reported issue (a bug, a broken deploy, an
unexpected production failure — anything beyond routine new-feature build-out),
add an entry to `docs/logbook.md` in the same session, before considering the fix
done. Entry shape (match the existing ones): symptom, investigation/root cause,
fix, and whether it deviates from `blueprint.md`/`implementation-guide.md` (call
this out explicitly, even if the answer is "no deviation" — that's still useful
signal). Dead-end attempts that didn't work are worth keeping too, so they aren't
retried blind later (see logbook #12 for the pattern).

**Why:** once the project is in day-to-day use, the logbook becomes a primary
source (alongside the blueprint/implementation-guide) for writing a final system
architecture document — it needs to be complete as we go, not reconstructed from
memory or git archaeology after the fact.

This does not apply to routine forward build-out of a phase's planned scope —
just to anything that came up as a problem and got fixed.

**Closing the loop is part of the task.** If an entry is logged as "held for
review," "not committed," or "pending" a decision, going back to confirm what
actually happened — it shipped, or it was declined — is part of finishing the
task, not a separate one. Don't leave the entry's closing status as the last
thing written if it later changes (see logbook #30/#32 for the failure mode
this prevents).

---

## Locked decisions — don't re-litigate these

- **Brand:** Women's Growth Society, masthead short form `WGS`, handle `@womensgrowthsociety`
- **Fonts:** Archivo Black (structural) + Alex Brush (script accent, one word/phrase only) + Inter (body/labels) — all free Google Fonts
- **Three duotone moods:** wisdom (violet) / bold (terracotta) / celebratory (gold) — see `WGS_BRAND_KIT` in Section 4/6 of the docs for exact hex values
- **Voice:** two registers (poetic, direct), resolved deterministically per-post via `APPROACH_REGISTER` — never guessed by the model
- **Models:** ~~Claude Haiku 4.5 (cheap tier) + Claude Sonnet 5 (strong tier)~~ — **reversed** (docs/logbook.md): `gpt-5.6-luna` (cheap tier) + `gpt-5.5` (strong tier) is now the default for the app's own text generation (`LLM_PROVIDER=openai`), driven by Anthropic running out of production credits plus real A/B evidence (`docs/direct-write-poc.md` Section 9) that gpt-5.5 outperformed Sonnet 5 on anchor authenticity and voice discipline. The Claude path is fully functional and unchanged, selectable per-call (`LLMProvider(provider="anthropic")`) or fleet-wide (`LLM_PROVIDER=anthropic` env var) — not removed, not degraded. GPT Image 2 for hero images, unchanged.
- **Rendering:** Satori via `@vercel/og`, on the frontend (Next.js route) — no headless browser, ever
- **Stack:** Next.js/React/Vercel (frontend) · FastAPI/Python/Railway (backend) · Supabase (DB + storage)

---

## Environment

- `backend/.env` and `frontend/.env.local` exist locally with real API keys already filled in — **do not recreate them or generate fresh `.env.example` placeholders that overwrite real values.**
- Both are correctly gitignored — verify with `git status` before any commit that they don't appear as staged.
- `IMAGE_QUALITY=low` — the medium→low experiment ran: duotoned output is visually indistinguishable, and low cut worst-case `/generate` latency roughly in half-to-third (100+s outliers down to a consistent ~20-32s) plus ~8x lower image cost. Switched on both Railway and local `.env`.
- **Frontend must build with `--webpack`, not Turbopack** (`frontend/package.json`'s `build` script is `next build --webpack`) — Next 16's default Turbopack production build silently skips the "Collecting build traces" step for this project, so the compiled `@vercel/og` binary Next's internal og module needs at runtime never makes it into the deployed Vercel serverless function. Every `/api/render` call 500'd in production until this was found (build succeeded and worked fine with `next dev`/local `next start` either way — this only ever showed up as a deployed-on-Vercel failure). Don't switch this back to Turbopack without re-verifying `/api/render` in production first.
- **Git integration is connected on both platforms and both are now trusted.** #20's outage (a docs-only push crashed the live service — `uvicorn: command not found` despite installing cleanly) was root-caused in #23, not just contained: `uv sync` was intermittently ignoring the pre-activated `/opt/venv` and installing into a separate `/app/.venv` instead (visible in build logs as a `VIRTUAL_ENV=/opt/venv does not match...` warning, present on every crash, absent on every success). Fixed by pinning `UV_PROJECT_ENVIRONMENT=/opt/venv` as a Railway service variable, forcing `uv` to always target the activated venv — verified by the warning's absence on the next build, not just a green deployment. `build.watchPatterns: ["/backend/**"]` in `railway.json` also means non-backend pushes (like this one) no longer trigger a Railway build attempt at all (confirmed: `SKIPPED`). Railway also has `NIXPACKS_UV_VERSION` pinned (currently `0.4.30`) for the same reason — without it, Nixpacks resolves `uv`'s own version via a live external lookup that can transiently fail. If a manual `railway up` is ever needed, run it from the **repo root**, not `backend/` — Root Directory being set for the git integration means a CLI upload from inside `backend/` now double-nests and fails. Full detail: `DEPLOY.md` §0, logbook #17-#23.
- The health-check gate (`deploy.healthcheckPath`/`healthcheckTimeout` in `railway.json`, added in #21) stays regardless — it's cheap insurance, not a sign auto-deploy is still untrusted.

---

## Working notes

- Bypass permissions mode is unreliable in this VS Code extension session (known extension bug) — expect occasional Bash prompts even with it enabled; this is fine, not worth fighting.
- Run `/clear` when starting a new phase, `/compact` when continuing deep into one — keeps context cost down. This file plus the two docs are enough to re-orient after either.
